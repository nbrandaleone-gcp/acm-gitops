kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: audit
  name: namespace-reader
  annotations:
    configsync.gke.io/cluster-name-selector: k3s
rules:
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["get", "watch", "list"]
